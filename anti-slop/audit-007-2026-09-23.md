# Denser farm vegetation

Scope: requested grass and flower density increase; existing farm artwork and
motion direction remain unchanged (ENERGY 2 / RHYTHM 2 / MOTION 2).

- Hard gate PASS: existing user-provided atlases only; no UI, copy, controls or
  navigation changes. The application runs and both scenery tests pass.
- Purpose gate PASS: 80-unit spacing and fewer empty cells fill bare areas;
  weighted grass frames favor vegetation over rocks, with occasional flowers.
- Liveliness PASS: existing flower and grass sway remains; the farm footprint
  stays clear so decorative density does not obscure gameplay.
- Craftsmanship PASS: generation and viewport caching share one spacing constant;
  deterministic placement and expanded-farm exclusion tests pass. The renderer
  still creates one scene object and only draws the visible area plus a margin.
