# Research export controls

Scope: the user requested one dataset export instead of JSON, quest CSV and DQN replay choices. Existing Dev Console styling, position, resize behavior and animations are retained. No new assets or layout system. The intentional focus is the dataset download, with storage clearing as a separate existing action.

- Hard Gate PASS for changed controls: one working JSON action; no fictional metrics or new assets. Null predictions now display Unavailable rather than a made-up 50% or 0%.
- Purpose Gate PASS: removed redundant export choices and replay counts because DQN is no longer the training method. Quest CSV is derived offline from the canonical JSON.
- Liveliness/consistency PASS within this small edit: existing console visual treatment and interaction behavior remain; no new decorative motion or theme.
- Craftsmanship PASS: browser click-through reached Dev Console > DDA > Download Dataset JSON and showed Export JSON: Completed. Screenshot confirmed the two controls fit the existing panel. Clear Stored Data was verified by unit test, not clicked against the user's 46 stored sessions. Production build and the full JavaScript suite passed.

This is a scoped review of the changed controls, not a claim that all existing UI accessibility issues are resolved. Build still reports pre-existing Svelte accessibility and bundle-size warnings.
