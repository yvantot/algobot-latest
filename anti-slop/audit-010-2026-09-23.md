# Expanded farm demonstration review

Direction: retain existing Algobot menus, teacher artwork and real Blockly
blocks. No new artwork. ENERGY 2 / RHYTHM 2 / MOTION 2.

- Timing is visible through water/wait blocks and actual bot waits. Wait is a
  default command, available in the practice toolbox without a reward gate.
- The teacher panel stays anchored to the bottom during its exit. Program
  panels retain entrance/exit transitions and disappear in scenes without code.
  Multi-bot scenes give each bot a labeled panel and independent highlights.
- First visits open the demonstration. Later game entries show onboarding until
  its existing opt-out is checked; Help still offers demonstration replay.
- Fire and pests have separate loss and rescue chapters. Demo-only fire spread
  and pest damage make the consequences predictable; normal event severity is
  still controlled by the existing event system.
- Expansion and speed upgrades affect only the example farm. The finale uses
  two concurrent roles, repeat blocks, a harvest check, timed growth and speech.
  It is an authored gameplay demonstration rather than student-code execution.
- Cloud entry starts farther outside the farm and uses an overshoot with a
  shallow rising arc. The initial pop and pause and final departure remain.
- Verification: 149 automated tests pass. A component integration test completes
  all 16 chapters using actual robot, crop, pest and fire behavior. It verifies
  no player rewards or farm dimensions change. Expansion cancellation/restoration
  tests pass, and all 40 research artifacts match their preserved baseline.
  Browser checks cover first/return visit routing, the default wait block and
  the water/wait demonstration. Full visual review of every new scene remains
  more limited than the automated runtime coverage.
