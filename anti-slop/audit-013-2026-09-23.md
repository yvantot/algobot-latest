# Editor resize and weather review

Direction: retain Algobot's existing gray frames, green controls and pixel art. ENERGY 2 / RHYTHM 2 / MOTION 2. Scope is the user's requested repairs.

1. Fixed Bot Command's clipped header and detached mode switch by positioning both editors and the switch together (R-26: controls must work). Both editors now have a labeled pointer resize handle with keyboard arrow support and cancellation cleanup.
2. Verified text editor dragging from 400 to 500 pixels at desktop width. At 786 pixels viewport width, verified Blockly dragging from 400 to 460 pixels and keyboard resizing to 480 pixels. The header and editor switch remain visible (R-03, R-34).
3. Event banners use the user's supplied fire and rain icons. No generated assets or new decorative elements (R-23).
4. Cloud horizontal approach and overshoot take 0.1 game seconds after the existing entrance pop and pause. Motion marks arrival at the target soil (R-19). Simulation and renderer tests cover the faster timing.

Validation: 160 tests pass; production build passes. Existing accessibility warnings in Documentation, Shop and Help, and the bundle-size warning remain outside this repair's scope.
