# Harvest check lesson review

Scope: the requested lesson before the if mission. Existing mission layout and artwork are reused; no new assets or visual styling.

- The lesson tells the player which blocks to connect and explains both possible answers in simple words (R-15). Text code is included as an alternative.
- The if mission requires claiming this lesson's reward first. Learning Progress includes the new lesson in Checks and choices (R-26).
- Both true and false sensor results earn credit after display; sensor errors and reading without displaying do not. Speech converts values to text before rendering.
- Validation: 162 tests pass, including asynchronous true/false outputs and error cases. Production build passes with existing accessibility and bundle warnings.
