# Camera limits and developer console

After-draft review under the existing preference. Direction: preserve the game's
existing developer console, with readable controls and grouped testing tools.
ENERGY 1 / RHYTHM 2 / MOTION 1; this is a utility panel over the farm.

- Hard gate PASS for this change: no invented assets, statistics or navigation;
  new actions use real handlers and validated numeric inputs. Tests cover invalid
  inputs and rejected asynchronous actions. Build and gameplay click-through pass.
- Purpose gate PASS: the existing dark panel distinguishes developer controls
  from player UI; only the header drags. Grouping follows simulation, events,
  reproducible setups, tile controls and diagnostics. No decorative imagery added.
- Liveliness PASS: the live farm remains visible behind the panel, active speed
  uses green, and the log reflects actual outcomes. A short entrance/exit motion
  identifies the tool opening; no continuous decorative motion is added.
- Craftsmanship PASS: larger log/section text, visible focus outlines, wrapped
  tabs and viewport-constrained sizing improve the existing console. Browser
  verification confirmed setup, cleanup, rejection feedback and state inspection.
  Camera bounds/zoom and JSON-safe diagnostics are covered by automated tests.

Scope excludes redesigning every older console tab and a full accessibility
audit of the game. No additional design changes require selection.
